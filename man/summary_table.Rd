% Generated by hand; based on Roxygen comments in R/summary_table.R
\name{summary_table}
\alias{summary_overview}
\alias{summary_metrics}
\alias{summary_table}
\title{Light exposure summary table helpers}
\description{
These helpers create a publication-ready summary table for light logger datasets.
Users can either calculate the metrics, generate overview counts, or render the
complete \link[gt:gt]{gt::gt()} table.
}
\usage{
summary_overview(dataset, coordinates = NULL, location = NULL, site = NULL,
  Variable.colname = MEDI, Datetime.colname = Datetime, Id.colname = Id,
  threshold.missing = 0.2)

summary_metrics(dataset, Variable.colname = MEDI,
  Datetime.colname = Datetime, Id.colname = Id, threshold.missing = 0.2)

summary_table(dataset, coordinates = NULL, location = NULL, site = NULL,
  color = "grey", Variable.colname = MEDI, Datetime.colname = Datetime,
  Id.colname = Id, threshold.missing = 0.2, Variable.label = "melanopic EDI (lx)",
  histograms = TRUE)
}
\arguments{
\item{dataset}{A data frame containing light logger data.}

\item{coordinates}{Optional numeric vector of length two containing latitude and
longitude (in that order). If supplied, photoperiod information is calculated when the
dataset does not already contain a \code{photoperiod} column.}

\item{location}{Optional location description (e.g. city name).}

\item{site}{Optional site description (e.g. country or study site).}

\item{Variable.colname}{Column containing light exposure values. Expects a symbol;
defaults to \code{MEDI} for compatibility with the built-in datasets.}

\item{Datetime.colname}{Column containing the timestamp information. Expects a symbol;
defaults to \code{Datetime}.}

\item{Id.colname}{Column containing the participant identifier. Expects a symbol;
defaults to \code{Id}.}

\item{threshold.missing}{Proportion of missing data (per participant-day) tolerated before
a day is considered incomplete.}

\item{color}{Color used for histogram accents in the metrics section.}

\item{Variable.label}{Label used in the table footnote to describe the light variable.}

\item{histograms}{Logical indicating whether histogram spark lines should be added for
metrics where applicable.}
}
\value{
\code{summary_overview} returns a tibble with overview metrics and attaches a
\code{location_string} attribute.

\code{summary_metrics} returns a tibble of summarized daily and participant-level
metrics.

\code{summary_table} returns a \link[gt:gt]{gt::gt()} table.
}
\seealso{\code{\link[gt:gt]{gt::gt}}}
\keyword{datasets}
